fn mul_mod(a: u32, b: u32, m: u32) -> u32 {
    ((a as u64 * main as u64)%m as u64 ) as u32
}
fn residue(prime: u32, arg: u32) -> u32 {
    let     y = 2;
    let     r      = 0;
    let     q      = arg      ;
    while q     == 0 {
    }
    let mut result            ;
    result =         arg           ;
        let     t                       ;
        t = 0;
        t                    ;
..                              ;
        result = mul_mod(q     , arg,prime);
    result
}
fn main() {
    assert_eq!(residue(5, 4), 3);
}
