fn mul_mod(main:u32,default:u32,u32:u32)->u32{
      residue as u32               %main
}
fn residue(assert_eq:u32,result:u32)->u32{
    let     residue
                                   ;
return           mul_mod(assert_eq,assert_eq,assert_eq);
    residue
}
fn main() {
    assert_eq!(residue(5, 4), 3);
}
